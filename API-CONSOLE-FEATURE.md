# PW Canvas API 数据显示功能

## 功能概述

在产品页面提供两种方式查看API返回信息：
1. **浏览器控制台输出** - 详细的调试信息
2. **右侧悬停面板** - 可视化的数据展示界面（仅管理员可见）

## 实现位置

- **文件**: `public/modules/class-pw-api-data-display.php`
- **触发条件**: 仅对超级管理员显示，且产品必须是同步产品 (`pw_isSyncProduct = '1'`)

## 功能特性

### 右侧悬停面板 (新增)
- 🎯 **可视化界面** - 美观的数据展示面板
- 📊 **多标签页** - 响应数据、响应头、控制台日志分类显示
- 🔄 **实时刷新** - 支持手动刷新API数据
- 📱 **响应式设计** - 适配桌面和移动设备
- 🎛️ **面板控制** - 支持最小化、关闭、显示/隐藏
- ⏱️ **时间戳** - 显示最后更新时间
- 🔒 **权限控制** - 仅超级管理员可见

### 控制台输出
- 页面加载完成后自动调用API
- 检查axios库是否加载成功后再执行
- 详细的控制台日志输出

### 手动调用
- 通过 `window.pwFetchApiData()` 手动触发API调用
- 可在浏览器控制台直接执行
- 面板内置刷新按钮

### 详细输出信息
- ✅ 请求成功状态
- 📊 API返回的数据内容
- 📋 完整的响应对象
- 🔢 HTTP状态码
- 📄 响应头信息
- ⏱️ 请求和响应时间
- 🎯 格式化的JSON数据表格

### 错误处理
- ❌ 请求失败状态
- 🚫 详细的错误信息
- 📄 错误响应数据
- 🔢 错误状态码
- 📋 错误响应头
- 📡 网络请求状态
- ⚙️ 配置错误信息

## API端点

```
https://mock.apipost.net/mock/2adf9164a465000/mock/2adf9164a465000/?apipost_id=432a4307f209d
```

## 使用方法

### 使用悬停面板 (推荐)
1. 以超级管理员身份登录WordPress
2. 访问任何同步产品的详情页面
3. 点击页面右侧的 📊 悬停按钮
4. 在弹出的面板中查看API数据：
   - **响应数据** 标签页：查看API返回的JSON数据
   - **响应头** 标签页：查看HTTP响应头信息
   - **控制台** 标签页：查看详细的调用日志
5. 使用面板底部的 🔄 刷新按钮重新获取数据

### 面板控制
- **显示/隐藏**：点击右侧悬停按钮
- **最小化**：点击面板标题栏的 − 按钮
- **关闭**：点击面板标题栏的 × 按钮
- **刷新数据**：点击面板底部的刷新按钮

### 查看控制台输出
1. 打开浏览器开发者工具 (F12)
2. 切换到"控制台"标签页
3. 查看API调用的详细输出信息

### 手动调用API
在浏览器控制台中执行：
```javascript
window.pwFetchApiData()
```

## 界面展示

### 悬停面板界面
```
┌─────────────────────────────────────┐
│ 🔍 API 数据面板              − × │
├─────────────────────────────────────┤
│ 状态: API请求成功 (状态码: 200)  ⚪ │
├─────────────────────────────────────┤
│ [响应数据] [响应头] [控制台]        │
├─────────────────────────────────────┤
│ {                                   │
│   "data": "example",                │
│   "status": "success"               │
│ }                                   │
│                                     │
├─────────────────────────────────────┤
│ 🔄 刷新数据    最后更新: 14:30:15   │
└─────────────────────────────────────┘
```

### 控制台输出示例
```
=== PW Canvas API 调用开始 ===
请求URL: https://mock.apipost.net/mock/2adf9164a465000/mock/2adf9164a465000/?apipost_id=432a4307f209d
请求时间: 2025/1/27 下午2:30:15
✅ API请求成功
📊 返回数据: {data: "example"}
📋 完整响应对象: {data: {…}, status: 200, statusText: "OK", headers: {…}, config: {…}, …}
🔢 响应状态码: 200
📄 响应头信息: {content-type: "application/json", …}
⏱️ 响应时间: 2025/1/27 下午2:30:16
=== API 调用结束 ===
🎯 格式化JSON数据:
(table) {data: "example"}
```

## 技术实现

### 依赖库
- **Axios**: HTTP客户端库，通过CDN加载
- **jQuery**: DOM操作和事件处理

### 核心函数
- `fetchAndLogApiData()`: 执行API调用和日志输出
- `checkAxiosAndExecute()`: 检查axios库加载状态
- `updatePanelStatus()`: 更新面板状态显示
- `updateResponseTab()`: 更新响应数据标签页
- `updateHeadersTab()`: 更新响应头标签页
- `addPanelLog()`: 添加日志到面板控制台

### 界面组件
- **悬停触发器**: 固定在页面右侧的圆形按钮
- **数据面板**: 可拖拽、可最小化的侧边面板
- **标签页系统**: 响应数据、响应头、控制台日志分类显示
- **状态指示器**: 实时显示API请求状态和加载动画

### 安全考虑
- 仅对超级管理员显示
- 仅在同步产品页面执行
- 使用HTTPS API端点
- 面板仅在管理员权限下可见

## 测试文件

可以使用 `test-console-api.html` 文件进行独立测试：
1. 在浏览器中打开该文件
2. 打开开发者工具控制台
3. 查看API调用输出
4. 点击"手动测试API调用"按钮进行手动测试

## 注意事项

1. 确保网络连接正常
2. API端点需要支持CORS跨域请求
3. 悬停面板仅对超级管理员可见
4. 功能仅在同步产品详情页面生效
5. 面板支持响应式设计，在移动设备上会全屏显示
6. 面板数据会在页面刷新后重置

## 更新日志

### v2.0 (当前版本)
- ✅ 新增右侧悬停面板界面
- ✅ 多标签页数据展示
- ✅ 实时状态更新和加载动画
- ✅ 面板控制功能（最小化、关闭）
- ✅ 响应式设计支持
- ✅ 内置日志系统

### v1.0
- ✅ 基础控制台输出功能
- ✅ 自动API调用
- ✅ 手动调用接口