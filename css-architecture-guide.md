# PW Canvas CSS 架构与开发规范

## 核心理念

为了确保项目的可维护性、可扩展性和团队协作效率，我们采用样式与逻辑分离的开发模式。所有样式代码必须在独立的代码库中进行管理和编译，主项目只负责引入最终生成的 CSS 文件。这种方式可以从根本上避免样式污染、冲突和难以维护的问题。

---

## 🚫 绝对禁止的做法

在主项目（PHP/HTML）的开发过程中，以下行为是被严格禁止的，任何违反规则的代码都将被视为不合规。

1.  **禁止使用内联样式（Inline Styles）**
    ```php
    // ❌ 错误示例
    echo '<div style="color: red; font-size: 14px;">这是一个错误的示范</div>';
    ```
    **理由**: 内联样式拥有最高的 CSS 优先级，会覆盖外部样式表的规则，导致样式难以调试和统一管理。

2.  **禁止在文件中使用 `<style>` 标签**
    ```html
    <!-- ❌ 错误示例 -->
    <style>
      .local-class {
        padding: 10px;
      }
    </style>
    ```
    **理由**: 将样式分散在各个文件中会造成代码碎片化，增加维护成本，并可能导致样式规则重复或冲突。

3.  **禁止在主项目中直接创建或修改 `.css` 文件**
    主项目的代码库中不应包含任何手写的 CSS 源代码。所有 CSS 都必须由外部编译流程生成。

---

## ✅ 正确的开发工作流

正确的样式开发和集成流程分为两部分：**样式开发（在 SCSS 仓库）** 和 **样式集成（在主项目）**。

### 1. 样式开发与编译 (在外部 SCSS 仓库)

* **只给scss**: 所有的样式，只用在chat界面，根据结构转为scss格式给我。
* **单一来源**: 所有的样式源文件（`.scss`）都必须存放在一个独立的、专门用于存放前端样式的 Git 仓库中。
* **使用 SCSS**: 必须使用 SCSS 编写样式，利用其变量、混入 (Mixin)、嵌套等高级功能来提高代码的复用性和可维护性。
* **命名规范**: 推荐使用 BEM (Block, Element, Modifier) 等命名约定，以创建结构化、可读性强的 CSS 类名。
* **编译流程**: 该样式仓库需要配置自动化工具（如 Webpack, Gulp, 或者 CI/CD 流程），将所有 SCSS 文件编译、合并、压缩成一个最终的 CSS 文件（例如 `pw-vue-components.css`）。

### 2. 样式集成与缓存控制 (在主项目)

当外部样式文件编译完成后，通过 `link` 标签将其引入到主项目的页面中。

* **引入方式**: 在 PHP 模板或 HTML 头部中，使用以下方式引入外部 CSS 文件。

    ```php
    <?php
      // 生产环境建议使用文件哈希或固定版本号
      // $version = 'hash_or_version_string'; 
    
      // 开发环境使用时间戳强制刷新缓存
      $version = time(); 
    ?>
    <link rel="stylesheet" href="[https://your-external-css-repo.com/dist/pw-vue-components.css?v=](https://your-external-css-repo.com/dist/pw-vue-components.css?v=)<?php echo $version; ?>">
    ```

* **缓存控制**:
    * **`?v=<?php echo time(); ?>`**: 这种方法通过在 URL 后面附加一个当前时间的UNIX时间戳作为查询参数，确保浏览器每次都会请求最新的文件，而不是使用本地缓存。
    * **适用场景**:
        * **开发环境**: 强烈推荐使用 `time()`，因为它可以确保你做的任何样式修改都能立即生效，无需手动清理浏览器缓存。
        * **生产环境**: 在生产环境中，更推荐使用基于文件内容的哈希值（File Hash）或固定的版本号（`v=1.2.3`）作为版本参数。这样可以在版本更新时才强制刷新缓存，而在版本稳定期间充分利用浏览器缓存，优化加载性能。

---

## 总结

简单来说，我们的 CSS 架构核心规则是：**“在主项目中，你是一名样式的‘消费者’，而不是‘生产者’。”** 所有的样式生产工作都必须在专门的外部流程中完成，主项目只负责消费最终的产物。