# 设计按钮与图片生成逻辑说明文档

## 概述

本文档详细分析了 `<button class="design-switch-btn active" data-tab="viewDesign">Design</button>` 按钮的点击行为以及整个图片生成逻辑。

## 按钮功能分析

### 设计按钮（Design）
- **按钮标识**: `data-tab="viewDesign"`
- **实际功能**: 仅切换UI标签页，**不生成图片**
- **作用**: 显示设计画布/编辑器界面，让用户进行设计创作

### 模拟图按钮（Mockups）
- **按钮标识**: `id="renderBtn" data-tab="viewMockup"`
- **实际功能**: 触发图片生成
- **作用**: 生成最终预览图片并打开新窗口展示

## 图片生成完整流程

### 1. 触发机制
```javascript
document.getElementById('renderBtn').addEventListener('click', async function () {
  const previewContainer = document.querySelector('.preview-canvas-container');
  const imageData = await (previewContainer ? capturePreviewCanvas() : captureCanvas());
  
  // 在新窗口打开生成的图片
  const previewWindow = window.open('', '_blank');
  previewWindow.document.write(`
    <html>
      <head><title>预览效果</title></head>
      <body>
        <img src="${imageData}" alt="预览效果">
      </body>
    </html>
  `);
});
```

### 2. 画布捕获方式

#### 方式一：预览模式捕获 (`capturePreviewCanvas`)
**使用场景**: 存在 `.preview-canvas-container` 时

**捕获图层顺序**:
1. 背景图层 (`bgLayer`) - 产品背景图
2. 阴影图层 (`shadowLayer`) - 阴影/高光效果
3. 预览画布 (`previewCanvas`) - 产品模板
4. 设计预览画布 (`designPreviewCanvas`) - 用户设计内容

**处理逻辑**:
- 创建临时画布进行合成
- 根据背景图尺寸计算缩放比例
- 所有图层居中放置
- 返回PNG格式数据URL

#### 方式二：设计模式捕获 (`captureCanvas`)
**使用场景**: 不存在预览容器时

**捕获图层顺序**:
1. 白色背景
2. 阴影图层 (`shadowCanvas`) - 3D效果
3. 颜色图层 (`colorCanvas`) - 产品基础颜色
4. 主画布 (`mainCanvas`) - 用户添加的文字和图片
5. 边界边距 (可选) - 打印安全区域

### 3. 动态画布检测

系统会根据当前激活的视图智能检测画布元素：

```javascript
function getActiveCanvasElements() {
  const store = window.useCanvasStore();
  if (store && store.activeViewId) {
    return {
      colorCanvas: document.getElementById(`colorLayer-${store.activeViewId}`),
      shadowCanvas: document.getElementById(`shadowLayer-${store.activeViewId}`),
      mainCanvas: document.getElementById(`mainCanvas-${store.activeViewId}`)
    };
  }
  // 回退到全局元素
}
```

## 图层合成细节

### 图层顺序与作用

| 图层顺序 | 图层名称 | 作用说明 |
|---|---|---|
| 1 | 白色背景 | 确保导出图片背景干净 |
| 2 | 产品颜色层 | 显示产品本身的颜色/图案 |
| 3 | 阴影层 | 提供3D立体效果 |
| 4 | 主设计层 | 用户添加的文字、图片、图形 |
| 5 | 边界线 | 打印安全区域标记（可选） |

### 技术规格

- **输出格式**: PNG
- **质量**: 100%
- **透明度**: 支持透明背景
- **尺寸**: 保持原始画布尺寸
- **比例**: 保持原始宽高比

## 使用场景对比

| 功能 | 设计按钮 | 模拟图按钮 |
|---|---|---|
| **主要作用** | 显示设计界面 | 生成预览图片 |
| **交互类型** | 界面切换 | 内容生成 |
| **输出结果** | 可编辑画布 | 静态图片 |
| **使用时机** | 设计创作阶段 | 设计完成预览 |

## 实际使用建议

### 正常操作流程
1. **点击Design按钮** - 进入设计模式进行创作
2. **完成设计后** - 点击Mockups按钮生成预览
3. **查看效果** - 在新窗口中查看最终效果

### 注意事项
- **设计按钮不会生成图片**，仅用于界面切换
- **模拟图按钮才是真正生成图片的功能**
- **生成的图片会在新标签页打开**，请确保浏览器允许弹窗
- **支持多视图系统**，会根据当前激活的视图生成对应图片

## 常见问题解答

**Q: 为什么点击Design按钮没有反应？**
A: Design按钮只负责切换界面，不会触发任何图片生成操作。

**Q: 如何查看设计效果？**
A: 完成设计后，点击旁边的